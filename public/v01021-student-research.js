(()=>{const pulses={w2s1_bigidea:{id:'early_clarity',q:'How are you feeling about this so far?',options:['Lost','Getting there','I get it']},w2s1_mapping:{id:'explain_confidence',q:'Could you explain this idea to someone else?',options:['Not yet','Probably','Yes']},w2s1_exit:{id:'experience',q:'How did today’s class experience help you learn?',options:['Less than a normal class','About the same','More'],comment:true}};let openId='';const esc=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function key(id){return`srpilot:${Classroom.session.id}:${id}`}async function send(p,value,comment=''){const r=await fetch(`/api/session/${encodeURIComponent(Classroom.session.id)}/research/pulse`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({pulse:p.id,question:p.q,value,comment,device:Classroom.device,name:localStorage.classroomName||'Anonymous',moment:Classroom.current()?.key||''})});if(!r.ok)throw Error('pulse failed');localStorage.setItem(key(p.id),'1')}function close(){document.querySelector('.sr121-pulse')?.remove();openId=''}function show(p){if(openId===p.id||localStorage.getItem(key(p.id)))return;openId=p.id;const d=document.createElement('div');d.className='sr121-pulse';d.innerHTML=`<section class="sr121-card" role="dialog" aria-modal="true" aria-label="Quick class pulse"><div class="sr121-kicker">10-second class pulse · not graded</div><h2>${esc(p.q)}</h2><div class="sr121-options">${p.options.map(o=>`<button data-v="${esc(o)}">${esc(o)}</button>`).join('')}</div>${p.comment?'<label class="sr121-comment">Optional: What should we change about this class experience?<textarea maxlength="500" placeholder="One thought, if you have one."></textarea></label>':''}<button class="sr121-skip">Skip</button><div class="sr121-status" aria-live="polite"></div></section>`;document.body.append(d);d.querySelector('.sr121-skip').onclick=()=>{localStorage.setItem(key(p.id),'skipped');close()};d.querySelectorAll('[data-v]').forEach(b=>b.onclick=async()=>{const status=d.querySelector('.sr121-status');status.textContent='Sending…';try{await send(p,b.dataset.v,d.querySelector('textarea')?.value.trim()||'');status.textContent='Thanks — look back up.';setTimeout(close,600)}catch(e){status.textContent='Did not send. Tap again.'}})}function tick(){try{if(!window.Classroom?.session||!Classroom.snapshot?.state)return;const p=pulses[Classroom.current()?.key];if(p)show(p)}catch(_){}}setInterval(tick,800)})();